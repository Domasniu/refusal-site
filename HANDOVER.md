# REFUSAL OS — 个人网站交接文档

> 最后更新：2026-08-22 · 仓库：https://github.com/Domasniu/refusal-site
> 线上地址：https://refusal-site.pages.dev/ · 站长后台：https://refusal-site.pages.dev/console/

---

## 〇、2026-08-22 更新记录（播放器/幻灯片/图片显示修复）

本次修复三个问题：
1. **音乐播放器不工作**：`main.js` 从未调用 `REFUSAL_PLAYER.init()`，播放器未初始化（歌单空、按钮无效）。已在 `initMusicPlayer()` 末尾补上初始化调用。同时 `player.js` 增加"播放失败自动跳下一首"（网易云外链失效不再卡死），并支持 `noAuto` 模式（进站不强制自动播放，符合浏览器策略）。
2. **首页分类模块幻灯片播放**：从"横向滚动条"升级为**真正的幻灯片**——一次显示一张大图 + 自动播放（4s/张，悬停暂停）+ 左右箭头 + 指示器圆点。控件在进入系统后初始化（`initCarousels()`，因为 hidden 容器测量为 0 会导致控件不生成）。
3. **图片裁剪严重**：作品卡片图片从 `height:210px; object-fit:cover` 改为**按原始比例完整显示**（`height:auto; max-height:340px; object-fit:contain`），竖图不再被截断；首页幻灯片大图区域用 `object-fit:contain` 完整展示。

**歌单清理**：`song-002.mp3` 与 `song-001.mp3` 为同一文件（sha256 一致，重复上传），已删除文件并从歌单移除；重复的网易云 Oceanside(3023862) 也移除。当前歌单：自传 Oceanside（song-001.mp3）+ 网易云海阔天空(347230)。

---

## 〇-2、2026-08-22 二次更新（播放进度/加载反馈/图片渐入/首页美化）

用户反馈"播放器不显示进度、播放时总在加载、图片加载慢、首页不美观"，本次修复：

1. **首页播放器进度不更新**：`player.js` 的 `syncProgress()` 此前只更新音乐面板和迷你播放器，**漏掉首页播放器**（`hmFill`/`hmCur`/`hmDur`）。已补上，首页"正在播放"现在正常显示时长和进度（已验证：播放后时间走动、进度条填充）。
2. **加载反馈**：`ensureAudio()` 增加 `waiting`/`stalled`/`canplay`/`playing` 事件，缓冲时显示"加载中…"，`preload` 从 metadata 改为 auto（自传 mp3 秒开）。
3. **图片加载优化**：首页轮播第一张 eager、其余 lazy（加速首屏）；所有作品图/动态图加载完成渐入（`img.loaded` 淡入，不再突兀闪现）；`slide-img` 区域加渐变底衬。
4. **首页美化**：
   - Hero 区：赛博网格背景 + 四角描边装饰（终端窗口感）
   - 统计卡片：加 emoji 图标 + 数字滚动动画 + hover 顶部光条
   - 正在播放：播放时整卡青色发光（`.home-music.playing`），封面仅在播放时旋转（`spinning` class）
   - 分类模块：标题加分类 emoji 图标（🎀✂️🎨📷…）
5. **数据同步**：合并了用户后台新增内容（作品 W-006 宠物 lucky、分类 pet 等），歌单仍为自传 Oceanside + 网易云海阔天空。

---

## 〇-3、2026-08-22 三次更新（首页加动态模块 / LIFE 页小红书风格）

用户反馈：首页只有作品展示、没有动态展示、浪费空间；参考小红书（RED）和 xinghuisama 博客首页设计。

1. **首页新增「最新动态 / LATEST MOMENTS」模块**：在"正在播放"和"作品分类"之间插入，展示最新 4 条动态的小红书风格图文卡片（图+文字+类型徽章+日期），点击图片打开灯箱、纯文字跳转 LIFE 面板。卡片数 <4 时 `auto-fit` 自适应列宽（2 张各占一半，不显窄）。
2. **LIFE 页改小红书风格**：从单列网格改为**双列瀑布流**（移动端 1 列）；卡片结构改为"头部（类型+日期）→ 大图 → 文字"（`.has-img` 用 order 排序），hover 抬升 + 卡片入场动画。
3. **空动态过滤**：`isEmptyMoment()` 过滤无文字且无图且无视频的空白动态（数据保留在 moments.json，后台可管理，渲染时跳过）。当前有效动态 2 条（M-002 图+文、M-003 图）。
4. 首页布局顺序：Hero → 统计卡片 → 正在播放 → **最新动态** → 作品分类幻灯片。

---

## 〇-4、2026-08-22 四次更新（动态图片加载修复 / 分类改紧凑卡片）

用户反馈"首页动态加载不出来、作品展示空白多很丑，参考淘宝卡片"：

1. **动态图片加载不出来**：`initImgFade()` 选择器漏掉 `.hm-moment-media`（首页动态容器），且**缓存图片的 `load` 事件早于 JS 绑定导致 `loaded` class 永不加上**（图片永远透明）。修复：选择器补全 + 轮询兜底（每 500ms 检查 `complete && naturalWidth>0` 直接显示）+ 立即检查。
2. **首页分类模块改淘宝式紧凑卡片**：从"一次一张大图幻灯片"改为 `cat-grid` 紧凑网格（1:1 方形缩略图 cover + 标题 + 分类徽章，`auto-fill` 自适应列数，一屏多张）。移除不再使用的轮播代码（`initHomeCarousel`/`carouselQueue`）。

---

## 〇-5、2026-08-22 五次更新（后台动态删除按钮修复）

用户反馈"空白动态无法删除"：后台 `console/index.html` 动态删除按钮生成时用了 `data-model` 属性，而事件绑定查询的是 `button[data-mdel]`——**属性名不匹配（model ≠ mdel），删除按钮从未绑定事件**。已改为 `data-mdel`，与绑定一致。

---

## 〇-6、2026-08-22 六次更新（首页两栏布局）

用户反馈首页不够美观，参考个人主页设计：

1. **首页改两栏布局**（`.home-layout`：左个人卡片 + 右内容区）：左栏 = 头像/名字/标签/统计/竖版音乐播放器/快捷导航；右栏 = 欢迎区（HELLO/标语/介绍/按钮）+ 最新动态 + 作品分类网格 + 联系便签。
2. 响应式：>1100px 两栏并排；≤1100px 单列（左栏置顶居中）。

---

## 〇-7、2026-08-22 七次更新（架构重构：全局左侧栏 + 内容全配置化）

用户提供交互图，要求**左侧栏切换页签时保持不动**、**所有内容支持配置**、**完全按参考图做**：

1. **全局左侧栏**（`.side-col`，独立于内容区，任何页签下不动）：文字式头部（`HELLO, I'M refusal·` + 状态"正在收集新灵感"）+ 音乐播放器 + **配置化导航**（`#side-nav`）+ 版本号。移动端折叠为顶部个人条 + 横向导航。
2. **内容全配置化**（`content/site.json`）：
   - `siteName`：站点名（顶栏 + 浏览器标题）
   - `hero`：`kicker`（HELLO 小字）/ `status`（侧栏状态）/ `slogan` / `desc` / `hashTags`（话题标签）
   - `nav`：导航项数组 `{id, label, icon, cat?}`——带 `cat` 的项（如宠物日常→pet、我的摄影→photo）点击后**直接跳作品区并自动筛选该分类**；"小游戏"项可配 `id` 指向任意面板
   - `homeModules`：首页模块数组 `{type, title, sub, cat?, sec?}`——`moments`（最新动态）/ `works`（作品，可按 cat 筛选）/ `note`（便签），可增删改排序
   - `contactEmail`：首页底部联系邮箱
   - `secTitles`：各面板标题
3. **首页右栏模块流**：欢迎区 → 最新动态 → 最新作品 → 宠物日常 → 摄影（无作品自动隐藏）→ A LITTLE NOTE 便签 → 留言卡片（"你好，欢迎来到我的作品集…收到啦！"）→ 联系我邮箱。
4. **console 后台**：主页文字区新增 站点名/欢迎语/侧栏状态/话题标签/小纸条/联系邮箱 输入框；保存时保留 nav/homeModules/secTitles/contactEmail。
5. **note 模块**：保留标题"A LITTLE NOTE"，内容不重复显示。

---

## 〇-8、2026-08-22 八次更新（后台重构 + 图片编辑/分级 + 动态多媒体）

### 1. 后台（console）大重构
- **分页签**：后台从"一长串"改为 **8 个顶层页签**：🎨作品 / 🐾宠物 / 📷摄影 / 💬动态 / 🎵音乐 / 🎬视频 / 🏠首页配置 / ⚙️站点设置；表单双列布局、容器加宽到 1180px。
- **宠物/摄影独立分区**：前端新增 `#pet`、`#photo` 两个独立面板；作品区筛选排除宠物/摄影；后台宠物/摄影各自独立页签，并支持**自由文本子分类标签**（如宠物名 lucky、摄影标签 风景/美食/人文），前台作品卡显示 `分类 · 子分类`。
- **可视化编辑**：侧栏导航(nav)、首页模块(homeModules)、面板标题(secTitles) 均可后台增删改排序；模块支持视频/音乐类型。
- **图片编辑器**：所有图片上传入口（作品/宠物/摄影/动态/头像）**选图后先编辑再上传**——裁剪(拖拽平移 + 比例预设)、旋转 90°、亮度/对比/饱和、滤镜(原图/黑白/复古/冷色/暖色/鲜艳)、恢复默认；多图逐张编辑。
- **图片分级**：上传自动生成 480px 缩略图(`-thumb.jpg`) + 1600px 大图；前台卡片用缩略图（旧图 `onerror` 自动回退原图）。
- 保存提示改为悬浮 toast（成功 5 秒自动消失）；缩略图点击=重新编辑、单独 ✕ 移除。

### 2. 动态（说说）升级
- **小红书风格**：桌面 4 列（响应式 3/2 列）、3:4 封面裁剪、文字 2 行省略、日期 + ❤点赞数、话题标签胶囊。
- **多视频**：动态 `video` 单字段 → `videos` 数组（自动迁移旧数据）；后台多视频逐条预览/移除。
- **灯箱混合媒体**：灯箱从"仅图片"升级为"图片 + 视频"混合，← → 切换、自动播放适配；动态多图多视频可切换查看。
- 视频卡片点击可播放（取消静音 + 显示控制条）。

### 3. 性能优化
- 图片缓存 1 小时 → 7 天（`_headers`，头像单独 no-cache）。
- 音乐 `preload` auto → metadata（进站不再预下载整首 mp3）。
- 现有 16 张旧图已批量生成缩略图（600KB 总）。

### 数据格式变化（新代码需注意）
- `site.json`：`hero` 新增 `titleScale`（标题字号缩放）；`homeModules` 项新增 `tag`（标签）；作品 `subcat`（宠物/摄影子分类）挂在 works 项上。
- `moments.json`：`video`（单）→ `videos`（数组）。
- 图片：大图 `xxx.jpg` + 缩略图 `xxx-thumb.jpg`（命名约定，前端 `thumbPath()` 推导）。

### 已知限制 / 待办
- 图片编辑器裁剪为鼠标拖拽，移动端触摸未做。
- 宠物/摄影子分类是自由文本（非固定下拉管理）。
- 灯箱自动播放对视频也是 3 秒切一张（可优化为跳过视频）。
- `buildHomeMomentCard` / `momentTypeLabel` 已是死代码，未清理。

---

## 〇-9、2026-08-22 九次更新（全站最新在前 + 卡片轮播 + 后台 UI 重构 + 顶栏/欢迎区美化）

### 1. 全站「最新在前」统一
- 首页四模块（生活动态/最新作品/宠物日常/摄影）、作品/宠物/摄影分区、LIFE 动态，统一按"最新在前"排序：动态按日期倒序（同天按添加倒序），作品按添加顺序倒序。
- 首页模块卡片与 LIFE 面板媒体顺序完全一致（修复"图片切换对不上"）；首页动态卡片同时包含视频。

### 2. 后台（console）再优化
- 首页模块类型新增「宠物日常 / 我的摄影」两个独立类型，移除冗余「分类(cat)」字段；改类型自动带出「查看全部」跳转目标。
- 作品/宠物/摄影/动态页：卡片化网格 + 主图 190px 大预览（点击重编辑、✕ 移除）+ 缩略图放大(64→84px) +「添加」按钮移到顶部（不用拉到底）+ 列表最新在前；动态页首图/视频大预览、可单独移除。
- 后台「一句话标语」改为多行文本域（支持换行分两行）。

### 3. 首页卡片放大 + 自动轮播
- 首页模块卡片尺寸可配置（compact/normal/large，默认 large），后台可选。
- 卡片自动轮播：图片 3s 切换、视频静音播放、播完再切（失败 3s 兜底跳过），带圆点指示器；后台可开关（homeCarousel）。

### 4. 顶栏 + 欢迎区美化
- 顶栏：站点名改粉色渐变 banner、管理按钮青色描边。
- 欢迎区：标语两行双色放大（第一行青色 / 第二行粉色，字号 2rem）；「新消息」气泡并入大标语卡片内右侧。

### 数据格式变化（新代码需注意）
- `site.json` 新增 `homeCardSize`（compact/normal/large）、`homeCarousel`（true/false）。
- `hero.slogan` 支持 `\n` 换行（第一行青色 / 第二行粉色）。
- `homeModules` 类型全集：`moments / works / pet / photo / video / music / note`；`cat` 字段已废弃（宠物/摄影为独立类型）。

### 已知限制 / 待办（新增）
- 旧版控制台保存会整体重写 site.json，可能丢失 homeCardSize/homeCarousel 等新字段（前端已做默认值兜底，新版后台保存会补回）。
- ~~moments.json 有两条重复 id（M-003）~~ 已在 〇-10 修复。

---

## 〇-10、2026-08-22 十次更新（欢迎区 kicker 修复 / 小游戏面板 / 灯箱视频轮播 / 数据与死代码清理）

### 1. 欢迎区 kicker 修复
- `index.html` 里 `id="hero-kicker"` 出现两次（侧栏 + 欢迎区），`getElementById` 只取到第一个，导致**欢迎区顶部的 "HELLO, I'M refusal·" 一直空白**。已把欢迎区元素改为 `id="wm-kicker"`，`main.js` 的 `renderHero()` 同时填充两处。
- `site.json` / `site-config.js` 的 kicker 原文 `HELLO,I'Mrefusal·` 缺空格，已统一改为 `HELLO, I'M refusal·`。

### 2. 新增「小游戏」面板（贪吃蛇）
- 新增 `#game` 面板：canvas 贪吃蛇 + 得分/最高分（localStorage 记忆 `refusal-snake-best`）+ 方向按钮（移动端）+ 键盘（方向键 / WASD 移动，空格暂停）。
- 侧栏导航"小游戏"由错误的 `id:home`（和"我的主页"重复）改为 `id:game`。
- 后台 console 的 `NAV_TARGETS`、`SEC_TITLE_KEYS` 补上"小游戏"，可在后台编辑。
- 离开小游戏面板自动暂停（`showSection` 里 `snPause()`）。

### 3. 灯箱自动播放对视频
- 修复"自动播放对视频也是 3 秒切一张（把视频切断）"：现在视频**静音播放、播完再切下一张**；播放失败或 3 秒内没播起来则跳过。图片仍 3 秒切换。

### 4. 数据清理
- `moments.json` 两条重复 id（M-003）已重排为 M-001~M-005（后台按序号编辑，不受影响）。

### 5. 死代码清理
- 删除未使用代码：`renderHomeStats`（无 `#home-stats` 元素）、`renderLatestWorks`、`buildHomeMomentCard`、`momentTypeLabel`、`CAT_ICONS`/`catIcon`。

---

## 〇-11、2026-08-22 十一次更新（侧栏名字放大 + 状态胶囊 + 页签标题字号配置）

按参考交互图微调：

1. **侧栏名字拆分放大**：`hero.kicker` 用换行拆成两段——第一行小字 "HELLO, I'M"（`.sk-hi`），第二行大字 "refusal·"（`.sk-name`，约 2.15rem，随 `hero.titleScale` 缩放）。数据改为 `"HELLO, I'M\nrefusal·"`。
2. **状态胶囊背景**：`.side-status`（"正在收集新灵感"）改为胶囊 pill —— 圆角背景 + 品红描边 + 前导呼吸小圆点。
3. **页签标题字号单独配置**：新增 `secTitleScale`（默认 1），通过 CSS 变量 `--sec-title-scale` 控制所有 `.sec-title` / `.home-sec-title`；后台 ③主页配置新增「所有页签标题字号」输入框并已接入保存。
4. **后台欢迎语改多行输入**：`f-kicker` 由单行 `<input>` 改为 `<textarea>`，可直接回车换行输入"HELLO, I'M / refusal·"两段。

### 数据格式变化
- `site.json` 新增顶层字段 `secTitleScale`（数字，默认 1）。
- `hero.kicker` 恢复为 `\n` 两段式：`HELLO, I'M` / `refusal·`。

---

## 〇-12、2026-08-22 十二次更新（作品/宠物/摄影支持视频 + 后台卡片瀑布流）

### 1. 作品 / 宠物 / 摄影支持上传视频
- 数据：`works.json` 作品项新增 `videos` 数组（与 `images` 并列，可选）。
- 后台：作品/宠物/摄影每张卡片新增「🎬 视频」按钮 + 视频预览/移除（复用动态的视频逻辑，上传到 `assets/works/video-XXX.mp4`）。
- 前台：作品卡显示 🎬 数量角标；点击作品打开灯箱为「图片 + 视频」混合切换（视频可播放）；首页作品/宠物/摄影模块卡片轮播也会带出视频。

### 2. 后台卡片改瀑布流（修复单图卡片下方大片空白）
- 后台作品/宠物/摄影/动态卡片网格从等高 CSS Grid 改为 CSS 多列瀑布流（`columns:320px` + `break-inside:avoid`），卡片按自身内容高度排列，不再因同一行里有「多视频/多图」的高卡片而把单图卡片拉出大片空白。

### 数据格式变化
- `works.json` 作品项新增 `videos`（字符串数组，可选）。

---

## 〇-13、2026-08-22 十三次更新（作品分类上移 + 宠物/摄影分类管理）

### 1. 作品分类上移
- 后台「作品分类」卡片移到「作品」卡片上方（⑩ 作品分类 → ⑪ 作品），上传很多作品后无需拉到底部即可添加分类。

### 2. 宠物 / 摄影分类管理
- 数据：`categories.json` 新增 `petCategories`、`photoCategories` 数组（`{id, label}`）。
- 后台宠物/摄影页签顶部各加一套分类管理（添加/删除、显示引用件数），宠物/摄影作品卡的"标签"从自由文本改为下拉选择（可选「无标签」）。
- 前台无需改动：作品卡仍显示「分类 · 子分类」（如 摄影 · 风景）。

### 数据格式变化
- `categories.json` 新增 `petCategories`、`photoCategories`（`{id,label}` 数组，可选，预置摄影 风景/美食/人文）。

---

## 〇-14、2026-08-22 十四次更新（作品/宠物/摄影改为「先建卡片再选图/视频」）

与「动态」保持一致：点「＋ 添加作品/宠物/摄影」先创建一个空卡片，再选传图片或视频，而不是一上来就弹选图。

1. **添加流程**：＋添加 → 空卡片（显示「添加图片或视频」占位）→ 点主图区选图作主图 /「＋ 加图」追加图片 /「🎬 视频」传视频。
2. **视频为主的作品**：无主图时，卡片大预览显示第一个视频；前台作品卡对「纯视频作品」显示 🎬 占位（不再裂图），点开灯箱直接播放视频。
3. 空作品点主图占位可直接选图；「＋ 加图」第一张图自动成为主图。

### 数据格式变化
- 作品项 `img` 允许为空字符串（纯视频作品）；`videos` 至少一个时前台仍可正常展示。

---

## 〇-15、2026-08-22 十五次更新（后台卡片改回「左到右」网格 + 不拉伸）

修正 〇-12 的瀑布流方案——CSS 多列是「从上到下」填充，导致排序不合预期、右侧留白。

1. 后台作品/宠物/摄影/动态卡片改回 **CSS Grid**（`auto-fill` + `align-items:start`）：保持「左到右、从上到下、最新在前」的排序。
2. `align-items:start` 让卡片按内容高度排列、不再拉伸，单图卡片不会再被同行的多视频/多图卡片拉出大片空白。
3. 移除之前瀑布流的 `columns` 写法。

---

## 〇-16、2026-08-22 十六次更新（宠物/摄影面板增加分类筛选）

宠物、摄影面板顶部现在和作品面板一样有分类筛选按钮：

1. 前台 `#pet`、`#photo` 面板新增 `#pet-filters`、`#photo-filters`，筛选按钮 =「全部」+ 该分区子分类（宠物名 / 摄影标签）。
2. 子分类来源：`categories.json` 的 `petCategories`/`photoCategories` 配置 + 作品实际用到的 `subcat` 去重合并，按配置顺序排列。
3. 点击筛选按钮即按子分类过滤该面板作品（最新在前）。

---

## 〇-17、2026-08-22 十七次更新（音乐播放器黑胶唱片化 + 封面支持）

参考网易云把 MUSIC 面板封面改成黑胶唱片风格：

1. **黑胶唱片封面**：`#p-cover` 改为圆形黑胶（外圈纹理 + 中心圆形标签 + 唱针/音臂），播放时唱片始终旋转、暂停即停；封面图显示在中心标签上（无封面则显示 🎵）。
2. **封面支持**：后台「音乐」页签每首歌新增「🖼️ 封面」上传 /「移除封面」，保存到 `assets/music/cover-XXX.jpg`；设置后 MUSIC 面板黑胶中心、侧栏迷你封面、悬浮播放器、歌单缩略图全部同步显示。
3. **歌单美化**：每首歌前面加圆形封面缩略图 + 时长（读取 audio 元数据）；超长歌名省略。
4. 歌词区加高、当前句高亮加粗。

### 数据格式变化
- `music.json` 歌曲项 `cover` 字段开始使用（填封面路径，空则 🎵 占位）。

---

## 〇-18、2026-08-22 十八次更新（歌词沉浸式大屏）★ 最新形态

1. MUSIC 面板控制条新增「词」按钮；点击打开**全屏歌词大屏**（覆盖整屏，模糊封面作背景 + 顶部歌名/歌手 + 居中歌词，当前句青色放大高亮、自动滚到中间）。
2. 无歌词时点「词」提示「暂无歌词」；关闭方式：右上 ✕ 或按 Esc。
3. 大屏歌词与播放进度实时同步（切歌/自动切歌自动重建），复用现有 LRC 解析。
4. 后台「音乐」页签已有歌词输入框（LRC 格式），填了歌词后即可启用大屏。

### 使用说明
- 后台「⑧ 音乐」→ 对应歌曲的「歌词」框粘贴 LRC 歌词，或点「⬆️ 上传 .lrc」直接传歌词文件（`[00:12.34]歌词` 格式）→ 保存 → 前台 MUSIC 面板点「词」看大屏。

---

## 〇-19、2026-08-22 十九次更新（修复 css/js 强缓存导致的「进不去/旧版」）★ 最新形态

### 问题
`_headers` 里 `css/*`、`js/*` 是 1 年 `immutable` 强缓存，而 `index.html` 每次更新的都是**同名文件**（`css/style.css`、`js/main.js`…），浏览器会一直用旧缓存，导致改完代码后访客看到的还是旧版（甚至开机画面卡住、点了进不去）。

### 修复
- `index.html` 里的 css/js 引用加了版本号 `?v=18`（`css/style.css?v=18`、`js/*.js?v=18`），URL 变了自然绕过旧缓存。
- **以后每次改 css/js 后，把 index.html 里的 `?v=18` 数字 +1**（`?v=19`、`?v=20`…），访客就会拉到新文件。

---

## 〇-20、2026-08-22 二十次更新（开机画面防卡死）

修复「卡在 REFUSAL 开机画面、终端空白、进不去」问题：

1. **内容加载加超时**：`tryFetch` 给每次请求加 6 秒超时，且 5 个 content JSON 改为**并行加载**——即使某个接口卡住，最多 6 秒后一定走默认配置进入 `init()`，不会再无限等待。
2. **进入按钮兜底**：`boot-enter` 的点击绑定提前到脚本加载时，并加 4 秒兜底强制显示「▶ TAP TO ENTER」——即使开机打字动画卡住，用户也能点按钮进入系统。
3. css/js 版本号升到 `?v=19`。

---

## 〇-21、2026-08-22 二十一次更新（修复 Out of Memory 崩溃）

修复手机/低内存浏览器打开首页报「错误代码: Out of Memory」：

1. **首页卡片轮播不再自动播放视频**：原来首页「最新动态/宠物/摄影」卡片轮播会自动静音播放视频，多个视频同时解码易导致内存崩溃。现在视频和图片一样按 3 秒切换、只显示首帧缩略图，点卡片打开灯箱再播放。
2. **修复播放器「播放失败自动跳下一首」的无限循环**：`errorCount` 不再在 `playIndex` 里被重置，改为在真正 `playing` 时才清零，避免某首歌加载失败时无限循环尝试。
3. css/js 版本号升到 `?v=20`。

---

## 〇-22、2026-08-22 二十二次更新（修复 LRC 歌词解析死循环 —— 真正的元凶）

**根因**：`player.js` 的 `parseLrc()` 里，`while (re.exec(raw))` 循环体内又调用了 `raw.replace(re, '')`——用的是**同一个带 `g` 标志的正则对象 `re`**，`replace` 会把它 `lastIndex` 重置为 0，导致 `exec` 永远匹配到第一个时间标签 → **死循环 + 无限往数组 push**。这就是「卡在开机画面 / Out of Memory / 页面无响应」的真正原因（之前歌曲歌词都是空的，一旦后台上传了 LRC 歌词就触发）。

**修复**：先把去掉时间标签的正文用**独立正则** `/\[[^\]]*\]/g` 一次性算出，`while` 循环只用 `re` 提取时间，两者不再共用 `lastIndex`，死循环消除。

- css/js 版本号升到 `?v=21`。

---

## 〇-23、2026-08-22 二十三次更新（音乐播放器参考网易云优化）

1. **双语歌词合并显示**：`parseLrc` 把相邻的英文+中文行合并成一行（英文为主、中文副），兼容「逐行对照」和「同时间戳」两种 LRC；面板歌词区、沉浸式大屏都显示「英文 + 中文副行」，当前句一起高亮。
2. **播放列表**：标题加歌曲数量 `（N）`；当前播放项用**跳动均衡条**（3 根柱子）替代序号，更有网易云感觉。
3. css/js 版本号升到 `?v=22`。

---

## 〇-24、2026-08-22 二十四次更新（手机端布局修复）

修复手机端（≤640px）UI 重叠/超框：

1. **顶栏精简**：手机端隐藏时钟、缩小站点名（超长省略号）、压缩搜索框/按钮间距——不再挤在一起或超框。
2. **侧栏改网格布局**：手机端侧栏用 CSS Grid 排成「头像+名字/状态 → 音乐播放器 → 导航」三行，消除头像/状态/播放器重叠遮挡。
3. 侧栏补 `margin-top:46px` 避开固定顶栏，头像不再被顶栏遮住；欢迎区小字降低字距、防溢出；css/js 版本号升到 `?v=24`。

---

## 〇-25、2026-08-22 二十五次更新（手机端作品网格两列 + 纯视频作品首帧预览）

1. **作品/宠物/摄影手机端改两列**：`.work-grid` 在 ≤640px 下改为 `repeat(2,1fr)`（与动态 LIFE 一致），卡片标题/分类纵向排列、隐藏描述，避免拥挤。
2. **纯视频作品显示首帧预览**：作品卡对「无主图但有视频」的作品改用 `<video preload="metadata">` 显示视频第一帧 + 居中 ▶ 播放角标（不再是 🎬 占位）。
3. css/js 版本号升到 `?v=25`。

---

## 〇-26、2026-08-22 二十六次更新（手机端作品卡缩略图改方形，消除大间距）

手机端作品/宠物/摄影卡片的缩略图（图片/视频/占位）统一改为 **1:1 方形 + `object-fit:cover`**，卡片高度一致，消除了原来「图片按原始比例 + 视频 4:3」导致的参差高度和大片空白间距。css 版本号升到 `?v=26`。

---

## 〇-27、2026-08-22 二十七次更新（作品/宠物/摄影与动态 LIFE 风格对齐）

1. **封面统一 3:4 竖版**：作品/宠物/摄影卡片的封面（图片/视频首帧/占位）从方形改为 **3:4 竖版 + cover**，与「动态 LIFE」卡片一致（桌面+手机都生效）。
2. **单视频作品点击内联播放**：和动态一致——纯视频（无图、仅 1 个视频）的作品，点卡片直接在卡内播放（取消静音 + 控制条），多图/多视频才打开灯箱。
3. css/js 版本号升到 `?v=27`。

---

## 〇-28、2026-08-22 二十八次更新（首页卡片支持手动滑动切换）

首页「最新动态/最新作品/宠物/摄影」卡片轮播现在支持手动切换：

1. **触摸滑动**（手机）：左右滑动切换上一张/下一张。
2. **鼠标拖拽**（桌面）：按住左右拖拽切换。
3. **圆点可点击**：点指示器圆点直接跳到对应张。
4. 手动切换会重置自动播放计时；滑动/点圆点不会误触「打开灯箱」。css/js 版本号升到 `?v=28`。

### 补充（同轮）
- 点开灯箱时改为**从当前正在播放的那张开始**（不再总是第一张）：`showLightbox` 支持起始下标，卡片记录当前 `idx`。js 版本号升到 `?v=29`。

---

## 一、项目概述

「REFUSAL OS」是 refusal· 的个人作品集网站，采用 **赛博朋克 OS 系统界面** 风格：
开机 BIOS 引导动画 → 进入主系统（顶栏 + 侧边导航 + 面板切换），展示手工发饰 / 剪纸 / 绘画 / 摄影等作品，含自定义音乐播放器、生活动态（说说）、站内搜索。

**技术栈**：纯静态 HTML/CSS/JS（无框架），数据驱动（`content/*.json`），托管于 Cloudflare Pages，后台通过 Cloudflare Pages Function + GitHub API 直写仓库实现内容管理。

---

## 二、目录结构

```
refusal-site/
├── index.html              # 主页面（开机画面 + OS 界面 + 搜索 + 播放器 + 灯箱）
├── _headers                # Cloudflare 缓存与安全头配置
├── robots.txt              # 搜索引擎规则
├── sitemap.xml             # 站点地图
├── css/
│   └── style.css           # 全部样式（赛博朋克主题、响应式、播放器、轮播、动态卡片）
├── js/
│   ├── palettes.js         # 主题色板（6 套配色，后台不修改）
│   ├── site-config.js      # 内置默认配置（本地双击预览回退值）
│   ├── player.js           # 自定义音乐播放器（audio 元素 + 自绘 UI）
│   └── main.js             # 核心交互逻辑（渲染、搜索、灯箱、导航、动态）
├── content/
│   ├── site.json           # 站点设置（头像/主题/主页文字/关于/联系/视频/音乐）
│   ├── works.json          # 作品列表（支持多图 images[]）
│   ├── categories.json     # 作品分类
│   ├── music.json          # 歌单（netease 外链 / file 自传 mp3）
│   └── moments.json        # 生活动态（文字/图片/视频）
├── assets/
│   ├── avatar.jpg          # 头像
│   ├── og-cover.png        # 社交分享图（1200×630）
│   ├── works/              # 作品图片
│   ├── music/              # 自传音乐文件
│   └── moments/            # 动态图片/视频
├── console/
│   └── index.html          # 站长后台（登录 + 内容管理）
└── functions/
    └── api.js              # Cloudflare Pages Function（后台保存接口）
```

---

## 三、核心功能说明

### 1. 开机引导
- 首次访问播放 BIOS 打字动画；返回访客自动跳过（sessionStorage 记忆）
- `?boot=1` 强制重看，`?skip=1` 强制跳过

### 2. 导航与面板
- **全局左侧栏**（`.side-col`，切换页签时保持不动）：文字式头部（HELLO + 状态）+ 音乐播放器 + **配置化导航**（`content/site.json` 的 `nav` 数组渲染，可增删改排序）
- 导航项 `{id, label, icon, cat?}`：`cat` 可选——带分类的项（宠物日常→pet、我的摄影→photo）点击后**直接跳作品区并自动筛选**；"小游戏"可配 `id` 指向任意面板或 `home`
- 面板切换基于 hash（如 `#works`），支持前进后退

### 3. 首页（配置化模块流）
- **欢迎区**：`hero.kicker`（HELLO 小字）+ slogan + desc + hashTags（话题标签）
- **配置化模块**（`homeModules` 数组驱动，可增删改排序）：
  - `moments`：最新动态（小红书风格图文卡片，最新 4 条）
  - `works`：作品紧凑网格（可按 `cat` 筛选，如宠物日常/摄影）
  - `note`：便签（如"A LITTLE NOTE"）
- 底部：留言卡片（"你好，欢迎来到我的作品集…收到啦！"）+ 联系邮箱（`contactEmail`）

### 4. 作品区
- 分类筛选按钮（全部 + 各分类，后台可增删）
- 作品卡片：**淘宝式紧凑卡片**（1:1 方形缩略图 cover + 标题 + 分类，`auto-fill` 自适应列数）
- **多图支持**：点击作品打开灯箱，多图时可左右切换（按钮 + 键盘 ← →），显示"2 / 3"计数

### 5. 站内搜索
- 顶栏 🔍 按钮 或 **Ctrl/Cmd + K** 打开
- 搜索范围：作品标题/描述/ID/分类名 + 动态文字
- 结果分组显示（分类 / 作品 / 动态），点击跳转

### 6. 音乐播放器（自定义）
- **歌单两种来源**：
  - `{ type: 'netease', id: '3023862' }` → 网易云外链播放（有版权限制，部分歌失效）
  - `{ type: 'file', url: 'assets/music/xxx.mp3' }` → 自传 mp3（推荐，稳定）
- 功能：播放/暂停、上下首、可拖拽进度条、音量/静音、循环/单曲/随机模式、可选 LRC 歌词
- **侧栏播放器**（全局左侧栏内，任何页签可见）+ 全局悬浮迷你播放器（左下角）+ MUSIC 面板完整播放器，三者同步
- 偏好记忆（localStorage：音量/模式）；播放失败自动跳下一首（网易云外链失效不卡死）

### 7. 生活动态（说说）
- LIFE 面板展示动态卡片：文字 + 图片（多图点开灯箱）+ 视频
- 按日期倒序排列

### 8. 灯箱
- 支持多图切换、左右箭头、键盘方向键、Esc 关闭、描述显示

---

## 四、站长后台（console/）

地址：`https://refusal-site.pages.dev/console/`

**登录**：输入后台密码（Cloudflare 环境变量 `CONSOLE_PASSWORD`），存 localStorage。

**功能**（登录后）：
1. **界面主题**：点色板切换 6 套配色
2. **头像**：上传替换（覆盖 `assets/avatar.jpg`）
3. **主页文字/首页配置**：站点名 / 欢迎语（kicker）/ 侧栏状态 / 标语标签 / 标语 / 介绍 / 话题标签 / 小纸条 / 联系邮箱
4. **侧栏导航（可视化编辑）**：增删改排序，每条可设图标 / 文字 / 跳转目标 / 分类筛选
5. **首页模块（可视化编辑）**：增删改排序，每块可设类型（动态/作品/便签）/ 标题 / 副标题 / 分类筛选 / 查看全部目标
6. **面板标题（可视化编辑）**：各页签顶部大标题
7. **自我介绍/联系**
8. **音乐**：填网易云歌曲 ID 或上传 mp3 文件，编辑歌名/歌手/歌词
9. **视频链接**：抖音/B站/YouTube 主页（留空显示"即将上线"）
10. **作品**：添加作品（传图）、**追加多图**（"＋加图"）、换主图、删图、编辑标题/分类/描述
11. **作品分类**：增删分类（有作品引用的分类不可删）
12. **生活动态**：添加动态、传多图、传视频、编辑文字/日期（删除按钮已修复）

> 导航（`nav`）、首页模块（`homeModules`）、面板标题（`secTitles`）均可在后台 ④⑤⑥ 可视化编辑，无需手改 site.json。

**保存**：点「💾 保存并发布」→ 通过 `/api`（GitHub API）写入仓库 → Cloudflare Pages 自动重新部署，1-2 分钟后生效。

---

## 五、部署与维护

### 部署架构
- **托管**：Cloudflare Pages（连接 GitHub 仓库 `Domasniu/refusal-site`，分支 `main`，构建命令无、输出目录根目录）
- **自动部署**：任何 push 到 main 触发重新构建；后台保存也是 push 到仓库 → 同样触发

### 环境变量（Cloudflare Pages → Settings → Environment variables）
| 变量 | 用途 |
|---|---|
| `GH_TOKEN` | GitHub 经典 PAT（repo 权限），后台读写仓库用 |
| `CONSOLE_PASSWORD` | 后台登录密码（可选，设置后必须带密码） |

### 缓存策略（_headers）
- `css/*`、`js/*`：一年强缓存 immutable
- `assets/*`：1 小时
- `content/*.json`：no-store（后台修改立即生效）
- `index.html`：no-cache

---

## 六、内容数据格式

### works.json（作品，支持多图）
```json
{
  "works": [
    {
      "id": "W-001",
      "cat": "handmade",
      "title": "蝴蝶结发箍",
      "img": "assets/works/handmade-1-headband.jpg",
      "images": ["assets/works/handmade-1-headband.jpg", "assets/works/其他图.jpg"],
      "desc": "描述文字"
    }
  ]
}
```
> `img` 为主图（必须），`images` 为全部图（含主图，可多张），`desc` 可选。

### categories.json（分类）
```json
{ "categories": [ { "id": "handmade", "label": "手工发饰" } ] }
```
> `id` 英文唯一标识，`label` 中文显示名。当前已有：手工发饰/剪纸/绘画/摄影/风景/人物/宠物。

### music.json（歌单）
```json
{ "playlist": [
  { "type": "netease", "id": "3023862", "title": "Oceanside", "artist": "Lainey Lou", "cover": "", "lrc": "" },
  { "type": "file", "url": "assets/music/song-001.mp3", "title": "歌名", "artist": "歌手", "cover": "", "lrc": "" }
] }
```
> `lrc` 为可选 LRC 歌词文本（`[00:12.34]歌词` 格式）。

### moments.json（动态）
```json
{ "moments": [
  {
    "id": "M-001",
    "type": "text",
    "date": "2026-08-22",
    "text": "文字内容",
    "images": ["assets/moments/img-001.jpg"],
    "video": ""
  }
] }
```
> type 自动判断：有 video → 视频；有 images → 图片；否则文字。视频示例：`"video": "assets/moments/video-001.mp4"`。

### site.json（站点设置）
```json
{
  "avatar": "assets/avatar.jpg",
  "theme": "cyber-cyan",
  "siteName": "refusal· 的宝藏仓库",
  "contactEmail": "hello@refusal.site",
  "hero": {
    "kicker": "HELLO, I'M refusal·",
    "status": "正在收集新灵感",
    "tags": "...", "slogan": "...", "desc": "...",
    "hashTags": "#手工 #视觉设计"
  },
  "nav": [
    { "id": "works", "label": "宠物日常", "icon": "🐾", "cat": "pet" }
  ],
  "homeModules": [
    { "type": "moments", "title": "最新动态", "sub": "记录日常，分享心情", "sec": "life" },
    { "type": "works", "title": "宠物日常", "sub": "记录日常", "cat": "pet", "sec": "works" },
    { "type": "note", "title": "A LITTLE NOTE", "sub": "希望记录自己，留下回忆。" }
  ],
  "note": "希望记录自己，留下回忆。",
  "secTitles": { "works": "我的作品 / WORKS" },
  "about": "自我介绍",
  "contact": "联系方式",
  "video": { "douyin": "", "bilibili": "", "youtube": "" },
  "music": {}
}
```
> `nav` = 左侧导航项（`cat` 可选：跳作品区并筛选该分类）；`homeModules` = 首页模块（`type`: moments/works/note；`cat` 可选；`sec` = "查看全部"跳转目标）。

---

## 七、主题配色

定义在 `js/palettes.js`，6 套：`cyber-cyan`（赛博青·默认）/ `neon-magenta`（霓虹品红）/ `violet-dream`（紫罗兰）/ `matrix-green`（矩阵绿）/ `amber-terminal`（琥珀）/ `blood-red`（血红）。
- 站长在后台切换（写入 site.json 的 theme）
- 访客点顶栏 🎨 循环切换（localStorage 覆盖，仅影响本人）

---

## 八、常见操作指南

| 想做什么 | 怎么做 |
|---|---|
| 增删改侧栏导航 | 后台 → ④侧栏导航 → 编辑/上移下移/删除/＋添加 → 保存 |
| 调整首页模块 | 后台 → ⑤首页模块 → 编辑/排序/删除/＋添加 → 保存 |
| 改面板标题 | 后台 → ⑥面板标题 → 编辑 → 保存 |
| 上传新作品 | 后台 → ⑩作品 → ＋添加作品 → 选图 → 填标题/分类 → 保存 |
| 给作品加第二张图 | 后台 → ⑩作品 → 该作品「＋加图」→ 多选图片 → 保存 |
| 添加风景/人物/宠物照片 | 后台 → ⑪分类 加新分类（或直接用已有）→ ⑩添加作品选该分类 → 保存 |
| 上传自己的音乐 | 后台 → ⑧音乐 → ⬆️上传音乐文件 → 保存（或直接放 assets/music/ 并改 music.json） |
| 添加网易云歌曲 | 后台 → ⑧音乐 → 填歌曲 ID → 保存（ID = 网易云歌曲页 URL 里 /song?id= 后的数字） |
| 发一条说说 | 后台 → ⑫生活动态 → ＋添加动态 → 写文字/传图/传视频 → 保存 |
| 换头像 | 后台 → ②头像 → 更换头像 → 保存 |
| 改主页文案 | 后台 → ③主页文字 → 保存 |
| 搜索站内内容 | 顶栏 🔍 或 Ctrl+K |
| 访客换配色 | 顶栏 🎨 |

---

## 九、已知限制与注意事项

1. **网易云外链有版权限制**：部分 VIP/付费歌曲外链失效（播放器提示"播放失败"）。自传 mp3 最稳定。
2. **GitHub 单文件限制 100MB**：上传大视频请压缩（建议 < 20MB，手机竖屏压缩）。
3. **后台保存是串行逐文件上传**：图片多时保存稍慢，属正常。
4. **console 登录密码存 localStorage**：本机浏览器记住密码，换设备需重输。
5. **部署有 1-2 分钟延迟**：后台保存后不要立刻刷新判断。
6. **`sameAs` JSON-LD 为空**：如有社交主页可填入 index.html 的 Person 结构化数据。
7. 音乐播放依赖浏览器自动播放策略：首次需用户点击播放（属正常浏览器限制）。

---

## 十、Git 工作流（开发用）

```bash
git clone https://github.com/Domasniu/refusal-site.git
cd refusal-site
# 改代码后
git add -A
git commit -m "描述改动"
git push origin main     # 触发 Cloudflare Pages 自动部署
```

> 注意：后台保存也会 push 到 main。**本地改动前先 `git pull`**，避免冲突。

---

## 十一、开发环境与工具链（本机）

- **本地仓库路径**：`D:\DeepseekHarness\refusal-site`
- **Node**：`C:\Program Files\nodejs\node.exe`（v24.19.0，dsh 需要 ≥ 22.19）
- **Git**：`C:\Program Files\Git\bin\git.exe`（仓库已配置身份 `Domasniu`）
- **视觉引擎（modlens）**：已配置千问 `qwen-vl-max`（OpenAI 兼容），配置在 `C:\Users\12619\.modlens\config.json`
  - 当前模型不支持直接读图，用 `npx @liustack/modlens analyze -i <图片路径>` 读图
  - 密钥轮换方式：`"<新key>" | npx @liustack/modlens config set openai.apiKey`
- **本机完整会话交接文档**：`D:\DeepseekHarness\SESSION_HANDOVER.md`（含凭证位置、未完成事项、常用命令，**未纳入本仓库**）

---

*文档由 DeepSeek Harness 协助编写，供 refusal· 本人或后续维护者使用。*
